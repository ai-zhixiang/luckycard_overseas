.venv/
__pycache__/
*.pyc
.env
# secrets & backups
.env.bak-*

# runtime data (wallet/quota ledger, prefs, db) — never public
/data/
app/data/

# challenge-authoring tools — contain the boon-enc algorithm + recipe helpers.
# NOT part of the site; must never be public (would hand crackers the transform set).
tools/

# user-generated content
app/static/uploads/
app/static/stylized/
app/static/temp_screenshot.png

# release artifacts / big binaries (hosted on the site, not in git)
app/static/luckycard-electron-*
app/static/WinDOS.bak.zip
app/static/*.exe
app/static/hugehard*
